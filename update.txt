【修正・対応済み】


## 2022年11月20日

- chart.jsのグラフを分単位で描画する
- 手動記録追加のDurationFieldの入力、例のJavaScriptライブラリ ( https://nadchif.github.io/html-duration-picker.js/ ) を使っても良いのでは？←プロジェクトの添付画像のようにする？

【モデル関係】
- 不要なフィールドの撤廃？(FoodMemoryのdescription、)
- 一部フィールドの文字数制限(FoodCategoryのname、FitnessCategoryのname)
- 体重と日付のデータを表示する(概要タブ)。グラフも作る

- 音量が出ないエラー(2022年11月18日確認 正常に音が鳴る)
- Bootstrapではないform系のタグの装飾の用意


【提案】

- 音量の調整欄(Cookieに保存する)

- DjangoMessageFrameworkを使って投稿した時に『投稿しました』と表示させる
- 他の投稿や削除もAjaxとレンダリングを行えるように仕立てる(includeするテンプレートファイルはディレクトリに格納する)

- テーマカラー(スキン)を用意して、黒背景に明るめの色で統一してbox-shadowなどの装飾で陰をつける、選べるようにする(CookieとDTLを使って選んだCSSをレンダリング)
- 円グラフが大きい順になっていないバグの修正

===========


- 要件定義書を作る





【修正・対応済み】

## 2022年11月2日

- タイマーとストップウォッチに不具合があったので修正
- 一部フォームにBootstrapを実装
- スマホ表示時に下部にメニューを実装。クリックして指定箇所にスクロールできる
- FitnessMemoryにtimeが0秒での投稿は禁止に修正
- フロッピーをクリックした時に保存、バツアイコンをクリックした時には保存せずに終了することにした。(ただしMenu実行時はいずれも保存されない)
- カレンダーの装飾を一部修正

## 2022年11月9日

- 日ごとの表示を横並び(フードとフィットネス両方) 画像はwidthとheightを固定しているので横並びにしてもガタガタになることは無い。
- フィットネスをやったらカレンダーにアイコンを表示させる(カレンダーの日付を左上に、アイコンを右側に並べる)
- メニューの時はフロッピーアイコンを消す、ストップウォッチの時はフロッピーアイコンを表示させる
- 今月の目標を表示(概要欄に今月の目標を記入する、トロフィーと違って特にギミックはないが利用者に目的意識を持ってもらうため)


## 2022年11月16日

- カレンダーの日付をクリックしたら日ごとのデータにジャンプする(aタグとidを使えば簡単に実現できる)
- 目標入力欄にて、ここはcheckboxを使ってフォームと目標の表示を切り替える形式にする
- トロフィー一覧の表示もフィットネスやフードと同じように横並び
- 体重と身長の記録



